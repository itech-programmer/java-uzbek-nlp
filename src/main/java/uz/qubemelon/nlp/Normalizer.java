package uz.qubemelon.nlp;

public class Normalizer {
    // Метод для нормализации текста (приведение к нижнему регистру)
    public String normalize(String text) {
        return text.toLowerCase();
    }
}